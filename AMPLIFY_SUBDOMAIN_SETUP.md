# Настройка поддомена в AWS Amplify

## 🔧 Настройка "Exclude root" / "Include root"

### Для поддомена `dev.ioperator.ai`:

**Установите: "Exclude root" ✅**

### Почему "Exclude root"?

1. **Корневой домен `ioperator.ai` уже используется** для production сайта
2. **Поддомен `dev.ioperator.ai`** - это отдельный dev-сайт
3. **Не нужно включать корневой домен** в эту domain association

### Что означает каждый вариант:

#### "Exclude root" (рекомендуется для вашего случая):
- ✅ Используется только поддомен: `dev.ioperator.ai`
- ✅ Корневой домен `ioperator.ai` НЕ включается
- ✅ Подходит для dev-окружения

#### "Include root":
- ⚠️ Включает и корневой домен `ioperator.ai`, и поддомен `dev.ioperator.ai`
- ⚠️ Может конфликтовать с production сайтом
- ⚠️ Не рекомендуется, если корневой домен уже используется

## 📋 Правильная настройка:

1. **Subdomain:** `dev`
2. **Exclude root:** ✅ Включено (исключить корневой домен)
3. **SSL Certificate:** Amplify managed certificate ✅

## ✅ После настройки:

1. Сохраните изменения
2. Amplify покажет DNS записи для добавления
3. Добавьте DNS записи в Hostinger
4. Подождите активации

**Установите "Exclude root" для поддомена dev!**

