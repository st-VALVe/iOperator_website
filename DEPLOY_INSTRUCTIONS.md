# 🚀 Инструкции по деплою

## Вариант 1: Автоматический деплой через GitHub Actions (РЕКОМЕНДУЕТСЯ) ✅

### Шаг 1: Добавьте GitHub Secrets

1. Откройте: https://github.com/st-VALVe/iOperator_website/settings/secrets/actions
2. Нажмите **"New repository secret"**
3. Добавьте следующие secrets:

**Secret 1:**
- **Name**: `AWS_ACCESS_KEY_ID`
- **Value**: `AKIAWR2CR5UETN3C56WN`

**Secret 2:**
- **Name**: `AWS_SECRET_ACCESS_KEY`
- **Value**: `CPNWqCicOo5EWtGicrXGBS6BmNwPA2XWTgwqR9IF`

**Secret 3:**
- **Name**: `AWS_S3_BUCKET_DEV`
- **Value**: `dev-ioperator-ai`

**Secret 4:**
- **Name**: `AWS_CLOUDFRONT_DISTRIBUTION_ID_DEV`
- **Value**: `E1FGI4F6OUJ05N`

### Шаг 2: Деплой

После добавления secrets, каждый push в ветку `dev` автоматически:
1. Соберет сайт (`npm run build`)
2. Задеплоит в S3
3. Инвалидирует CloudFront cache

**Или запустите вручную:**
- GitHub → Actions → "Deploy Dev to AWS" → "Run workflow"
- ⚠️ **Важно**: При ручном запуске выберите ветку **`dev`**, не `main`!

## Вариант 2: Локальный деплой

### Если у вас установлен Node.js:

```bash
# Установить зависимости (один раз)
npm install

# Собрать сайт
npm run build

# Задеплоить
python deploy_to_s3.py
```

### Если Node.js не установлен:

1. **Установите Node.js**: https://nodejs.org/
   - Скачайте LTS версию
   - Установите (npm будет включен)
   
2. **После установки**:
```bash
npm install
npm run build
python deploy_to_s3.py
```

## Вариант 3: Деплой через AWS CLI напрямую

Если у вас уже есть собранный `dist/`:

```bash
# Деплой в S3
aws s3 sync dist/ s3://dev-ioperator-ai --delete

# Инвалидация CloudFront
aws cloudfront create-invalidation --distribution-id E1FGI4F6OUJ05N --paths "/*"
```

## ✅ Рекомендация

**Используйте GitHub Actions!** Это самый простой способ:
- Не нужно устанавливать Node.js локально
- Автоматический деплой при каждом push
- Все происходит в облаке

Просто добавьте secrets и всё будет работать автоматически! 🎉

