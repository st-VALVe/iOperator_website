# Полное исправление DNS - Финальная инструкция

## 🔍 Текущая ситуация

DNS все еще не работает. Давайте исправим все по порядку.

## ✅ ЧТО ДОЛЖНО БЫТЬ В HOSTINGER DNS

### Запись 1: SSL Verification CNAME
```
Type: CNAME
Host: _d1f679a16a0445b0c6bf349cb89fd7d2.dev
Points to: _a019048cfc4c0e41eebbb40d3213ec76.jkddzztszm.acm-validations.aws
TTL: 14400
```

### Запись 2: Основной поддомен CNAME (КРИТИЧНО!)
```
Type: CNAME
Host: dev
Points to: d1aiijv4wsebaq.cloudfront.net  ← ЭТО НОВОЕ ЗНАЧЕНИЕ!
TTL: 300
```

**ПРОВЕРЬТЕ:** Если там `d11wekb7rhfeem.cloudfront.net` - ОБЯЗАТЕЛЬНО обновите!

### Запись 3: CAA для Amazon
```
Type: CAA
Host: @
Flag: 0
Tag: issue
CA domain: amazontrust.com  (только это, без кавычек!)
TTL: 14400
```

## 🔧 ПОШАГОВОЕ ИСПРАВЛЕНИЕ

### Шаг 1: Проверьте CNAME для dev

В Hostinger DNS:
1. Найдите CNAME запись с Host = `dev`
2. Посмотрите значение "Points to"
3. **Если `d11wekb7rhfeem.cloudfront.net`** → нужно обновить
4. **Если `d1aiijv4wsebaq.cloudfront.net`** → правильно

### Шаг 2: Обновите CNAME (если нужно)

1. Нажмите **"Edit"** на записи `dev` → CNAME
2. В поле **"Points to"** удалите старое значение
3. Введите новое: `d1aiijv4wsebaq.cloudfront.net`
4. **Сохраните**

### Шаг 3: Удалите старую запись (если есть дубликат)

Если есть ДВЕ CNAME записи для `dev`:
- Одна со старым значением `d11wekb7rhfeem.cloudfront.net` → **УДАЛИТЕ**
- Одна с новым значением `d1aiijv4wsebaq.cloudfront.net` → оставьте

### Шаг 4: Проверьте CAA запись

1. Найдите CAA запись для `@`
2. Откройте для редактирования
3. В поле **"CA domain"** должно быть только: `amazontrust.com`
4. **БЕЗ кавычек**
5. **БЕЗ "0 issue"**
6. Если неправильно - исправьте и сохраните

### Шаг 5: Подождите и проверьте

1. **Подождите 15-20 минут** после изменений
2. Проверьте через онлайн:
   - https://www.whatsmydns.net/#CNAME/dev.ioperator.ai
   - Должно показывать: `d1aiijv4wsebaq.cloudfront.net` из всех локаций

3. В AWS Amplify → Custom domains
4. Статус должен измениться автоматически

## 🆘 Если все еще не работает

### Вариант A: Полная переустановка

1. **В AWS Amplify:**
   - Custom domains → Actions → Delete domain
   - Подтвердите удаление
   - Подождите 5 минут

2. **В Hostinger:**
   - Удалите ВСЕ DNS записи для `dev`:
     - CNAME: `dev` → (любое)
     - CNAME: `_d1f679a16a0445b0c6bf349cb89fd7d2.dev` → (любое)

3. **В AWS Amplify:**
   - Add domain → `dev.ioperator.ai`
   - Manual configuration
   - Configure domain
   - Скопируйте НОВЫЕ DNS записи
   - Добавьте их в Hostinger
   - Add domain

4. Подождите 30-60 минут

### Вариант B: Использовать Route 53

Если Hostinger продолжает создавать проблемы:
1. Перенесите DNS в AWS Route 53
2. Это упростит интеграцию с Amplify
3. Один клик настройка

## 📊 Проверка после исправления

```bash
# Проверка DNS
nslookup dev.ioperator.ai

# Должно показать:
# Name: d1aiijv4wsebaq.cloudfront.net

# Проверка доступности
curl -I https://dev.ioperator.ai
```

## 💡 Важные моменты

1. **CloudFront domain изменился** - обязательно обновите CNAME
2. **CAA запись** должна быть правильного формата
3. **Подождите достаточно времени** (15-20 минут минимум)
4. **Проверьте через онлайн инструменты** - они показывают реальный статус

## 🎯 Критический чек-лист

Отметьте каждый пункт:

- [ ] CNAME для `dev` указывает на `d1aiijv4wsebaq.cloudfront.net`
- [ ] НЕТ старой записи `dev` → `d11wekb7rhfeem.cloudfront.net`
- [ ] CAA запись для `@` содержит только `amazontrust.com` (без кавычек)
- [ ] SSL verification CNAME правильная
- [ ] Прошло 15-20 минут после изменений
- [ ] Онлайн проверка показывает правильное значение

Если все отмечено, но не работает - попробуйте полную переустановку (Вариант A).

