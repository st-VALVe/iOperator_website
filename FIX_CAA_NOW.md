# СРОЧНОЕ ИСПРАВЛЕНИЕ: CAA запись для поддомена dev

## 🚨 Проблема

CAA запись добавлена для корневого домена `ioperator.ai`, но Amplify проверяет CAA для поддомена `dev.ioperator.ai`.

**Решение: Добавьте CAA запись специально для поддомена `dev`!**

## ✅ ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС

### Шаг 1: Добавьте CAA запись для поддомена dev

В Hostinger DNS добавьте **НОВУЮ** CAA запись:

1. **Нажмите "Add record"** или "+"
2. Заполните:
   - **Type**: `CAA`
   - **Host**: `dev` (НЕ `@`!)
   - **Flag**: `0`
   - **Tag**: `issue`
   - **CA domain**: `amazontrust.com` (БЕЗ кавычек, БЕЗ "0 issue")
   - **TTL**: `14400` (или 300)

3. **Сохраните**

### Шаг 2: Проверьте существующую CAA запись

Убедитесь, что CAA запись для корневого домена (`@`) тоже правильная:

- **Type**: CAA
- **Host**: `@`
- **Flag**: `0`
- **Tag**: `issue`
- **CA domain**: `amazontrust.com` (только домен, без кавычек и префиксов)
- **TTL**: 14400

Если в поле "CA domain" есть что-то кроме `amazontrust.com` (например, "0 issue "amazontrust.com""), **отредактируйте** и оставьте только `amazontrust.com`.

### Шаг 3: Подождите 10-15 минут

После добавления CAA записи для `dev` подождите 10-15 минут для распространения DNS.

### Шаг 4: Нажмите Retry в Amplify

1. В AWS Amplify → Custom domains
2. Найдите домен `dev.ioperator.ai`
3. Нажмите кнопку **"Retry"**

## 📋 Итоговые CAA записи в Hostinger

После добавления у вас должно быть **2 CAA записи для Amazon**:

1. **Для корневого домена:**
   - Host: `@`
   - CA domain: `amazontrust.com`

2. **Для поддомена dev (НОВАЯ!):**
   - Host: `dev`
   - CA domain: `amazontrust.com`

## 🔍 Почему это важно

Amplify проверяет CAA записи для конкретного поддомена `dev.ioperator.ai`. Если CAA запись есть только для корневого домена, поддомен может наследовать ее, но иногда это не работает сразу.

Добавление CAA записи напрямую для `dev` гарантирует, что Amplify увидит разрешение для Amazon.

## ✅ После исправления

1. Добавьте CAA для `dev`
2. Подождите 10-15 минут
3. Нажмите Retry в Amplify
4. Подождите 25-50 минут для активации

После этого домен должен активироваться!

## 🎯 КРИТИЧЕСКИ ВАЖНО

**Добавьте CAA запись с Host = `dev` (не `@`)!**

Это ключевое отличие - Amplify проверяет CAA для поддомена, а не только для корневого домена.

