# 🔧 Исправление проблемы с workflow

## ❌ Проблема:

GitHub показывает предупреждение:
> "Workflow does not exist or does not have a workflow_dispatch trigger in this branch"

## ✅ Причина:

Workflow файл `.github/workflows/deploy-aws-dev.yml` был изменен, но изменения не закоммичены в ветку `dev`.

## ✅ Решение:

### Шаг 1: Переключиться на ветку dev
```bash
git checkout dev
```

### Шаг 2: Добавить и закоммитить workflow
```bash
git add .github/workflows/deploy-aws-dev.yml
git commit -m "Update deploy-aws-dev workflow: fix region to eu-north-1"
```

### Шаг 3: Запушить в GitHub
```bash
git push origin dev
```

## ✅ После этого:

1. Workflow появится в ветке `dev`
2. Можно будет запустить его вручную через "Run workflow"
3. Автоматический запуск будет работать при push в `dev`

## 📋 Проверка:

После push, обновите страницу GitHub Actions - предупреждение должно исчезнуть!

