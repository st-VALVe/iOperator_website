# ⚡ Быстрый деплой

## 🎯 Самый простой способ: GitHub Actions

### 1. Добавьте Secrets в GitHub (2 минуты)

**URL**: https://github.com/st-VALVe/iOperator_website/settings/secrets/actions

Добавьте 4 secrets:

| Name | Value |
|------|-------|
| `AWS_ACCESS_KEY_ID` | `AKIAWR2CR5UETN3C56WN` |
| `AWS_SECRET_ACCESS_KEY` | `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` |
| `AWS_S3_BUCKET_DEV` | `dev-ioperator-ai` |
| `AWS_CLOUDFRONT_DISTRIBUTION_ID_DEV` | `E1FGI4F6OUJ05N` |

### 2. Деплой

**Вариант A: Автоматически**
- Просто сделайте push в ветку `dev`
- GitHub Actions автоматически задеплоит

**Вариант B: Вручную**
- GitHub → Actions → "Deploy Dev to AWS"
- Нажмите "Run workflow" → **Выберите ветку `dev`** → Run
- ⚠️ **Важно**: Выбирайте ветку **`dev`**, не `main`!

## ✅ Готово!

После деплоя сайт будет доступен через CloudFront.

## 📋 Что происходит автоматически:

1. ✅ Checkout кода
2. ✅ Установка зависимостей (`npm ci`)
3. ✅ Сборка сайта (`npm run build`)
4. ✅ Деплой в S3 (`aws s3 sync`)
5. ✅ Инвалидация CloudFront cache

**Ничего устанавливать локально не нужно!** 🎉

