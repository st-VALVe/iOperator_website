# 📋 Информация о ветках для деплоя

## ✅ Ответ: Используйте ветку **`dev`**

### Почему `dev`?

Workflow "Deploy Dev to AWS" настроен на:
- **Автоматический запуск**: При push в ветку `dev`
- **Ручной запуск**: Можно запустить вручную, но нужно выбрать ветку `dev`

### Конфигурация workflow:

```yaml
on:
  push:
    branches:
      - dev        # ← Автоматически запускается только для dev
  workflow_dispatch:  # ← Ручной запуск
```

### Как запустить вручную:

1. GitHub → **Actions**
2. Выберите workflow: **"Deploy Dev to AWS"**
3. Нажмите **"Run workflow"**
4. В выпадающем списке **"Use workflow from"** выберите: **`dev`**
5. Нажмите **"Run workflow"**

### ⚠️ Важно:

- **НЕ используйте ветку `main`** для этого workflow
- Workflow для `main` - это другой workflow (Deploy to GitHub Pages)
- Workflow "Deploy Dev to AWS" предназначен только для ветки `dev`

### Автоматический запуск:

Если вы просто делаете push в ветку `dev`, workflow запустится автоматически - ничего делать не нужно!

```bash
git checkout dev
git add .
git commit -m "Update"
git push origin dev
# ← Workflow запустится автоматически!
```

