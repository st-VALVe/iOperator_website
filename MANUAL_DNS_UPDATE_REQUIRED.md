# ⚠️ Ручное обновление DNS требуется

## Проблема с Hostinger API

API ключ возвращает **403 Forbidden** при попытке обновить DNS записи.

**Возможные причины:**
1. API ключ не имеет прав на управление DNS
2. Нужны дополнительные разрешения в Hostinger
3. API endpoint требует другого формата

## 🔧 РУЧНОЕ ОБНОВЛЕНИЕ (ОБЯЗАТЕЛЬНО!)

### В Hostinger DNS:

1. **Войдите в Hostinger панель управления**
2. **Перейдите в DNS** для домена `ioperator.ai`
3. **Найдите CNAME запись для `dev`**
4. **Отредактируйте:**
   - Нажмите "Edit"
   - В поле **"Points to"** измените на: `d2jvtvpqhbx36v.cloudfront.net`
   - Сохраните

### Или удалите и создайте заново:

1. **Удалите** старую CNAME запись для `dev`
2. **Добавьте новую:**
   - Type: **CNAME**
   - Host: **dev**
   - Points to: **d2jvtvpqhbx36v.cloudfront.net**
   - TTL: **300**
3. **Сохраните**

## ⏳ После обновления:

1. **Подождите 10-15 минут** для распространения DNS
2. **В AWS Amplify Console:**
   - Custom domains → `dev.ioperator.ai`
   - Нажмите **"Retry"** для активации домена
3. **Подождите 15-30 минут** для активации

## ✅ Проверка:

После обновления запустите:
```bash
python automate_full_dns.py
```

Он покажет обновленный статус.

## 📊 Текущие значения:

**Старое (в Hostinger сейчас):**
```
CNAME dev -> d1aiijv4wsebaq.cloudfront.net  ❌
```

**Новое (нужно установить):**
```
CNAME dev -> d2jvtvpqhbx36v.cloudfront.net  ✅
```

**Обновите DNS вручную в Hostinger!**

