# СРОЧНОЕ ИСПРАВЛЕНИЕ: dev.ioperator.ai не работает

## 🚨 Проблема

DNS не резолвится для `dev.ioperator.ai` - домен не может быть найден.

## ✅ ПРОВЕРЬТЕ ЭТО ПРЯМО СЕЙЧАС

### 1. Проверка DNS записей в Hostinger (КРИТИЧНО!)

**Войдите в Hostinger → DNS для `ioperator.ai`**

**Должны быть ТОЛЬКО эти записи для `dev`:**

```
✅ CNAME: _d1f679a16a0445b0c6bf349cb89fd7d2.dev -> _a019048cfc4c0e41eebbb40d3213ec76.jkddzztszm.acm-validations.aws
✅ CNAME: dev -> d11wekb7rhfeem.cloudfront.net
```

**НЕ должно быть:**
```
❌ CNAME: dev -> st-VALVe.github.io  (УДАЛИТЕ ЭТУ ЗАПИСЬ!)
❌ A запись для dev (если есть - удалите)
```

### 2. Онлайн проверка DNS (САМЫЙ НАДЕЖНЫЙ СПОСОБ)

Откройте в браузере:
**https://www.whatsmydns.net/#CNAME/dev.ioperator.ai**

**Что должно быть:**
- Если показывает `d11wekb7rhfeem.cloudfront.net` → DNS работает правильно
- Если показывает `st-VALVe.github.io` → **СТАРАЯ ЗАПИСЬ НЕ УДАЛЕНА!**
- Если показывает "Not found" → DNS еще не распространился или запись не добавлена

### 3. Проверка статуса в AWS Amplify

1. Откройте **AWS Amplify Console**
2. Перейдите в **Domain management**
3. Проверьте статус домена `dev.ioperator.ai`

**Возможные статусы:**
- **"Verifying domain ownership"** → Подождите 15-30 минут
- **"SSL certificate requested"** → Подождите 15-30 минут
- **"Active"** → Домен должен работать, проверьте деплой
- **"Failed"** или **"Error"** → Проблема с DNS, проверьте записи

### 4. Проверка последнего деплоя

1. В AWS Amplify → ваш app
2. Проверьте последний деплой:
   - Должен быть деплой из ветки `dev`
   - Статус должен быть **"Succeeded"**
   - Если нет деплоя → сделайте push в ветку `dev`

## 🔧 ЧТО ДЕЛАТЬ СЕЙЧАС

### Вариант A: Старая DNS запись не удалена

**Симптомы:**
- Онлайн проверка показывает `st-VALVe.github.io`
- В Hostinger есть две записи для `dev`

**Решение:**
1. В Hostinger DNS найдите запись `dev -> st-VALVe.github.io`
2. **УДАЛИТЕ эту запись**
3. Оставьте только `dev -> d11wekb7rhfeem.cloudfront.net`
4. Подождите 10-30 минут
5. Проверьте снова

### Вариант B: DNS запись не добавлена

**Симптомы:**
- Онлайн проверка показывает "Not found"
- В Hostinger нет записи `dev -> d11wekb7rhfeem.cloudfront.net`

**Решение:**
1. В Hostinger DNS добавьте CNAME:
   - Имя: `dev`
   - Points To: `d11wekb7rhfeem.cloudfront.net`
   - TTL: 300
2. Сохраните
3. Подождите 10-30 минут
4. Проверьте снова

### Вариант C: DNS еще не распространился

**Симптомы:**
- Записи настроены правильно
- Онлайн проверка показывает разные результаты из разных локаций
- Прошло меньше 1 часа

**Решение:**
1. Подождите еще 30-60 минут
2. Проверьте через онлайн инструменты
3. Очистите DNS кеш: `ipconfig /flushdns` (Windows)

### Вариант D: Нет деплоя в Amplify

**Симптомы:**
- DNS работает правильно
- Но сайт не открывается
- В Amplify нет успешного деплоя

**Решение:**
1. Убедитесь, что есть код в ветке `dev`
2. Сделайте push в ветку `dev`:
   ```bash
   git checkout dev
   git push origin dev
   ```
3. Подождите деплоя в Amplify (2-5 минут)
4. Проверьте сайт снова

## 📊 Быстрая диагностика

Выполните эти команды:

```bash
# 1. Проверка DNS локально
nslookup dev.ioperator.ai

# 2. Проверка онлайн (откройте в браузере)
# https://www.whatsmydns.net/#CNAME/dev.ioperator.ai

# 3. Очистка DNS кеша
ipconfig /flushdns

# 4. Проверка доступности
python check_dev_availability.py
```

## 🎯 Чек-лист

Отметьте каждый пункт:

- [ ] В Hostinger есть CNAME: `dev -> d11wekb7rhfeem.cloudfront.net`
- [ ] В Hostinger НЕТ записи: `dev -> st-VALVe.github.io`
- [ ] Онлайн проверка показывает `d11wekb7rhfeem.cloudfront.net`
- [ ] В AWS Amplify есть успешный деплой из ветки `dev`
- [ ] Статус домена в Amplify не "Failed"
- [ ] Прошло достаточно времени (30-60 минут)

## 🆘 Если ничего не помогает

1. **Удалите домен в AWS Amplify:**
   - Domain management → Delete domain

2. **Проверьте DNS записи в Hostinger:**
   - Убедитесь, что только правильные записи

3. **Добавьте домен заново в Amplify:**
   - Add domain → `dev.ioperator.ai`
   - Manual configuration
   - Скопируйте новые DNS записи

4. **Обновите DNS в Hostinger:**
   - Используйте новые значения от Amplify

## ⏰ Временные рамки

- **Минимум**: 30 минут после правильной настройки DNS
- **Обычно**: 1 час
- **Максимум**: 2 часа

Если прошло больше 2 часов и ничего не работает → проблема с настройкой, проверьте все пункты выше.

