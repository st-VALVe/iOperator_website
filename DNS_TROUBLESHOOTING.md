# Диагностика проблем с dev.ioperator.ai

## 🔍 Текущая проблема

DNS не резолвится для `dev.ioperator.ai` - домен не может быть найден.

## 📋 Пошаговая диагностика

### Шаг 1: Проверка DNS записей в Hostinger

1. **Войдите в Hostinger** → DNS для домена `ioperator.ai`
2. **Проверьте наличие записей:**

   Должны быть следующие CNAME записи:

   ```
   _d1f679a16a0445b0c6bf349cb89fd7d2.dev  →  _a019048cfc4c0e41eebbb40d3213ec76.jkddzztszm.acm-validations.aws.
   dev                                    →  d11wekb7rhfeem.cloudfront.net
   ```

3. **Проверьте правильность значений:**
   - Имена записей должны быть точно как указано
   - Content/Value должны совпадать с значениями от Amplify
   - TTL может быть любым (300 или Auto)

### Шаг 2: Проверка через онлайн инструменты

Используйте эти инструменты для проверки DNS:

1. **DNS Checker**: https://www.whatsmydns.net/#CNAME/dev.ioperator.ai
2. **MXToolbox**: https://mxtoolbox.com/SuperTool.aspx?action=cname%3adev.ioperator.ai
3. **DNS Propagation**: https://dnschecker.org/#CNAME/dev.ioperator.ai

### Шаг 3: Проверка статуса в AWS Amplify

1. **Откройте AWS Amplify Console**
2. **Перейдите в Domain management**
3. **Проверьте статус домена `dev.ioperator.ai`:**
   - Должен быть статус: "Pending verification", "Verifying", или "Active"
   - Если статус "Failed" или "Error" - проверьте DNS записи

### Шаг 4: Проверка через командную строку

```bash
# Проверка основного поддомена
nslookup dev.ioperator.ai

# Проверка SSL verification записи
nslookup _d1f679a16a0445b0c6bf349cb89fd7d2.dev.ioperator.ai

# Проверка CloudFront
nslookup d11wekb7rhfeem.cloudfront.net
```

## 🔧 Возможные проблемы и решения

### Проблема 1: DNS записи не добавлены

**Симптомы:**
- DNS не резолвится
- Нет записей в Hostinger

**Решение:**
1. Добавьте все DNS записи из `HOSTINGER_DNS_SETUP.md`
2. Подождите 10-60 минут для распространения DNS
3. Проверьте снова

### Проблема 2: Неправильные значения в DNS

**Симптомы:**
- DNS резолвится, но на неправильный адрес
- Сайт не открывается

**Решение:**
1. Проверьте точность значений в Hostinger
2. Убедитесь, что:
   - Имя записи: `dev` (не `dev.ioperator.ai`)
   - Content: `d11wekb7rhfeem.cloudfront.net` (точное значение)
   - Для SSL verification: точка в конце обязательна

### Проблема 3: Старая запись не удалена

**Симптомы:**
- DNS указывает на `st-VALVe.github.io` вместо CloudFront

**Решение:**
1. Найдите CNAME запись для `dev` с Content `st-VALVe.github.io`
2. **Удалите** эту запись
3. Убедитесь, что есть запись с Content `d11wekb7rhfeem.cloudfront.net`
4. Подождите 10-60 минут

### Проблема 4: DNS еще не распространился

**Симптомы:**
- Записи добавлены правильно
- Но DNS еще не резолвится

**Решение:**
1. Подождите 10-60 минут (иногда до 24 часов)
2. Проверьте через онлайн инструменты (см. Шаг 2)
3. Очистите DNS кеш на вашем компьютере:
   ```bash
   # Windows
   ipconfig /flushdns
   
   # Linux/Mac
   sudo systemd-resolve --flush-caches
   ```

### Проблема 5: Неправильное имя записи в Hostinger

**Симптомы:**
- Запись добавлена, но не работает

**Решение:**
- В Hostinger в поле "Имя" указывайте только `dev` (не `dev.ioperator.ai`)
- Hostinger автоматически добавит `.ioperator.ai`
- Для SSL verification: `_d1f679a16a0445b0c6bf349cb89fd7d2.dev`

## ✅ Правильная конфигурация DNS в Hostinger

Должны быть следующие записи:

```
Тип: CNAME
Имя: _d1f679a16a0445b0c6bf349cb89fd7d2.dev
Content: _a019048cfc4c0e41eebbb40d3213ec76.jkddzztszm.acm-validations.aws.
TTL: 300

Тип: CNAME
Имя: dev
Content: d11wekb7rhfeem.cloudfront.net
TTL: 300
```

## 🔍 Проверка после исправления

После исправления DNS записей:

1. **Подождите 10-60 минут**
2. **Проверьте через онлайн инструменты** (см. Шаг 2)
3. **Проверьте в AWS Amplify** → Domain management
4. **Проверьте доступность:**
   ```bash
   python check_dev_availability.py
   ```

## 📞 Если проблема не решается

1. **Проверьте логи в AWS Amplify:**
   - Domain management → View details
   - Проверьте ошибки проверки DNS

2. **Свяжитесь с поддержкой Hostinger:**
   - Убедитесь, что DNS записи правильно сохранены
   - Проверьте, нет ли ограничений на CNAME записи

3. **Проверьте статус в AWS Amplify:**
   - Убедитесь, что домен правильно добавлен
   - Проверьте, что SSL сертификат запрашивается

## 🎯 Быстрая проверка

Выполните эти команды для быстрой диагностики:

```bash
# Проверка DNS
nslookup dev.ioperator.ai

# Проверка SSL verification
nslookup _d1f679a16a0445b0c6bf349cb89fd7d2.dev.ioperator.ai

# Проверка CloudFront
nslookup d11wekb7rhfeem.cloudfront.net

# Проверка доступности
curl -I https://dev.ioperator.ai
```

