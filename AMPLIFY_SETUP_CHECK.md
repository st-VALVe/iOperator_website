# Проверка настройки AWS Amplify для dev.ioperator.ai

## ✅ Что правильно настроено (по скриншоту)

1. **Домен**: `dev.ioperator.ai` указан корректно
2. **Subdomain**: `https://dev.ioperator.ai` добавлен
3. **SSL Certificate**: "Amplify managed certificate" выбран (правильно)
4. **Manual configuration**: Выбрано (правильно для Free Tier)

## ⚠️ Что нужно проверить

### 1. Нажата ли кнопка "Add domain"?

Если вы еще не нажали кнопку **"Add domain"** внизу экрана:
- **Нажмите "Add domain"** чтобы завершить настройку
- После этого Amplify покажет DNS записи для настройки в Hostinger

### 2. Настроены ли DNS записи в Hostinger?

После нажатия "Add domain", Amplify покажет DNS записи. Проверьте:

**Текущий статус DNS:**
- `dev.ioperator.ai` сейчас указывает на `st-VALVe.github.io` (GitHub Pages)
- Нужно изменить на DNS записи от Amplify

**Что нужно сделать:**
1. В Hostinger DNS найдите CNAME запись для `dev`
2. Измените Content с `st-VALVe.github.io` на значение от Amplify (например: `xxxxx.amplifyapp.com`)
3. Или добавьте новую CNAME запись, если ее нет

### 3. Статус домена в Amplify

После настройки DNS проверьте статус в Amplify:
- Перейдите в **Domain management**
- Проверьте статус домена `dev.ioperator.ai`
- Должен быть статус: "Pending verification" или "Active"

## 📋 Пошаговая проверка

### Шаг 1: Завершите настройку в Amplify
- Убедитесь, что нажата кнопка **"Add domain"**
- Скопируйте DNS записи, которые покажет Amplify

### Шаг 2: Обновите DNS в Hostinger
- Войдите в Hostinger → DNS для `ioperator.ai`
- Найдите или создайте CNAME запись для `dev`
- Измените Content на значение от Amplify
- Сохраните

### Шаг 3: Проверьте статус
- Подождите 10-60 минут для проверки DNS
- Проверьте статус в Amplify → Domain management
- Должен появиться статус проверки DNS

### Шаг 4: Ожидание SSL
- После успешной проверки DNS Amplify запросит SSL
- Это займет 15-30 минут
- Статус можно отслеживать в Domain management

## 🔍 Текущий статус (проверено)

- **DNS**: `dev.ioperator.ai` → `st-VALVe.github.io` (нужно изменить на Amplify)
- **HTTPS**: Не доступен (SSL сертификат еще не выдан)
- **Amplify**: Настройка начата, но DNS еще не настроен

## ✅ Следующие действия

1. **Нажмите "Add domain"** в Amplify (если еще не нажали)
2. **Скопируйте DNS записи** от Amplify
3. **Обновите DNS в Hostinger** с новыми значениями
4. **Подождите 10-60 минут** для проверки
5. **Проверьте статус** в Amplify → Domain management

После этого сайт будет доступен на https://dev.ioperator.ai


